import React from 'react';

const ViewerPage = () => (
  <div className="page-card">
    <h2>Viewer Page</h2>
    <p>Viewers can read content and access the dashboard, but cannot change application data.</p>
  </div>
);

export default ViewerPage;
